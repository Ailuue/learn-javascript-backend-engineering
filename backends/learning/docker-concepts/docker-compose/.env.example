# Copy to .env and fill in values. .env is git-ignored — never commit real creds.
# Compose auto-loads .env and substitutes ${VAR} in docker-compose.yml.
POSTGRES_DB=appdb
POSTGRES_PASSWORD=localpass
APP_ENV=development
