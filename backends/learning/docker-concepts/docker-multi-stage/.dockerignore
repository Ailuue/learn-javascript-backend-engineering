# Keeps the build context small — Docker sends this to the daemon before building.
# Excluding things here speeds up builds and prevents accidental cache busts.
node_modules/
npm-debug.log
.env
.git/
notes*.js
*.md
